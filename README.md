# Hanrose Atelier

Premium kidswear boutique website built with Vite, React, Tailwind CSS, and Supabase.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates a production build.
- `npm run ship` builds, commits, and pushes confirmed changes to the active Git branch.
